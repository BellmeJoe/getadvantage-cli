# Session log — 2026-07-19 · v1

| Field | Value |
|--------|--------|
| **ID** | `2026-07-19-v1` |
| **Date** | 2026-07-19 |
| **Version** | v1 (first export of this conversation; increment if you re-export after more work) |
| **Tool** | Grok Build (Grok 4.5) · TUI |
| **Primary repo** | `C:\Users\ben\projects\getadvantage-cli` |
| **Also covered** | `getadvantage`, `plusxplus`, Grok TUI prefs |
| **Status** | Uncommitted CLI work (0.7.1 + 0.7.2); **no** publish / push / tag / deploy |
| **Next file** | If continuing same day after more work → `2026-07-19-v2-SESSION.md` |

> **Purpose:** `/export` did not work in the TUI. This file is the handoff document for
> another session. Point the new session at **this path** first.

**Lead the next session with:**

```
Read C:\Users\ben\projects\getadvantage-cli\docs\sessions\2026-07-19-v1-SESSION.md
then docs/REVIEW-0.7.1.md and docs/RELEASE-0.7.2-REVIEW.md.
Confirm npm test (34/34). Do not publish/push/tag/deploy without me.
```

---

## 1. Conversation arc (what we did, in order)

### 1.1 Grok product / UI

- User asked whether there is a **better UI than CLI** for Grok Build.
- Answer: primary product is a **full-screen TUI**, not a bare CLI; no official
  desktop GUI for Build. Options: themes, IDE via ACP (`grok agent stdio`),
  custom WebSocket clients, grok.com chat (different product).
- User asked to **see Tokyo Night** → mock HTML preview at
  `C:\Users\ben\.grok\tokyo-night-preview.html` (opened in browser).
- User asked to **enable it** → set in `C:\Users\ben\.grok\config.toml`:

  ```toml
  [ui]
  theme = "tokyonight"
  ```

  (Needs truecolor terminal.)

### 1.2 getAdvantage product screen

- User: screen `projects/getadvantage`, then continue work later.
- **Canonical path:** `C:\Users\ben\projects\getadvantage`
- **What it is:** AEO/GEO SaaS — scan how ChatGPT/Claude/Perplexity/Gemini read a
  site, score AI-readability, paste-ready fixes, monitoring, Ray, Stripe, Neon.
- **Live:** https://getadvantage.app · Vercel `getadvantage` · package name still
  `fluentweb` (legacy).
- **Stack:** Next 16, React 19, npm, Neon/PGlite, jose/bcrypt, Stripe, `proxy.ts`.
- **Strategy drift (important):** docs disagree — AEO hero vs Ship-Safe/Trust Layer
  vs two-doors-in-one-repo vs plus×plus split. Treat code + founder intent as truth.
- **Git mess:** `main` was ahead 2 / behind 83 origin; dirty tree; many
  `getadvantage-*` / `ga-*` worktrees under `projects/`.
- User attitude: **cleanup of worktree sprawl is low priority** (“who cares”) unless
  it blocks shipping or wrong-cwd deploys.

### 1.3 plus×plus repo screen

- Dedicated repo: `C:\Users\ben\projects\plusxplus` · GitHub `BellmeJoe/plusxplus`
- **Split from getAdvantage 2026-06-28** · Vercel `plusxplus` · **plusxplus.de**
- Shares **only** Neon prod DB + Stripe with getAdvantage; crons stay on getAdvantage.
- **Focus:** German sales-led AEO + **`/sales` Operator** (Marc feedback, outreach,
  pipeline). Git healthier than getadvantage (`main` ≈ origin).
- July docs thesis: **Trust + Operator** — getAdvantage = Trust/Ship Gate;
  plus×plus `/sales` = first Operator product.
- STRUCTURE.md inside plusxplus still partially stale (old two-doors wording);
  CLAUDE/HANDOFF/PRODUCT-ROADMAP are the real story.

### 1.4 Export / copy conversation

- First answer: use `/export` (and `/copy` for last reply).
- User: **export doesn’t work** → write this dated/versioned document instead.
- Convention going forward: `docs/sessions/YYYY-MM-DD-vN-SESSION.md` in the
  relevant product repo (CLI for this workstream).

### 1.5 getAdvantage CLI work (bulk of the session)

**Resume prompt used:**

> Continue the getAdvantage CLI. Read `docs/SESSION-HANDOFF-0.7.1.md` and
> `ROADMAP.md`, confirm `npm test` is green, then start 0.7.2 top-down (begin
> with `brief` preserving manual edits). Do not publish, push, tag, or deploy
> without me.

**Repo:** `C:\Users\ben\projects\getadvantage-cli` · npm `getadvantage`

#### Confirmed baseline

- 0.7.1 trust fixes already in working tree (uncommitted).
- `npm test` was **29/29** green, then **30/30**, then **34/34** after 0.7.2 work.
- Package version end state: **0.7.2** in `package.json`.

#### 0.7.1 (already implemented before/during handoff — trust-critical)

| Finding | Fix |
|---------|-----|
| `gate-utf16-false-go` | BOM UTF-16 decode before binary skip |
| `gate-nonascii-filename-skipped` | `gitFilesZ` / `-z` |
| `gate-npx-tsc-thirdparty` | Local `typescript/bin/tsc` only; never `npx --yes tsc` |
| `gate-ship-go-on-corrupt-packagejson` | `checkManifest` NO-GO |
| `demo-requires-git-repo` | `demo` before repo-root gate |
| `ci-workflow-unpinned` | Install deps + pin version in generated Action |

#### 0.7.2 (implemented this session — honesty + branding + polish)

| Finding | Fix |
|---------|-----|
| `brief-eats-manual-edits` | Protected notes block; foreign brief refuse |
| `shipsafe-brand-residue` | Write `getadvantage_*` markers; read legacy ship-safe |
| `shipsafe-name-collision` | README teaches only `getadvantage` |
| `map-vs-check-routes` | Shared `scanRoutes` + stack detect; Next `src/app/api` |
| `switch-cursorrules-claim` | Honest Cursor tip |
| `missing-brief-called-stale` | Separate missing vs stale copy |
| `fanout-wont-collide-overclaim` | Share brain / fan-in catches collisions |
| `deploy-token-in-argv` | Token via child env |
| `mcp-schema-not-enforced` | Validate args → -32602 |
| `self-artifacts-trip-dirty-guard` | Brain files not “scratch” alone |
| Polish | gitSafe stderr, did-you-mean, login abort exit 1, map `--json`, build 10m timeout, author hello@getadvantage.app, demo help math |

User asked to **do both remaining steps at high capability** (map/check unify + full polish) — completed; tests **34/34**.

#### Review artifacts written (for another session)

| Path | Purpose |
|------|---------|
| `docs/REVIEW-0.7.1.md` | **Independent 0.7.1 review → SHIP** |
| `docs/RELEASE-0.7.2-REVIEW.md` | Combined release pack, checklist, risks |
| `docs/COMMIT-MSG-0.7.2.txt` | `git commit -F` message |
| `docs/SESSION-HANDOFF-0.7.1.md` | Updated with review pointer |
| `docs/SESSION-HANDOFF-0.7.2.md` | Engineering handoff + dual resume prompts |
| `ROADMAP.md` | 0.7.1 + 0.7.2 marked code-complete |
| **This file** | Full conversation continuity |

#### 0.7.1 review outcome (dedicated evaluation)

- **Verdict: SHIP** all six trust fixes.
- No release-blocking defects on cold-QA repros.
- Residuals (non-blocking): UTF-16 without BOM; missing tsc = warn+GO; no unit
  test for workflow YAML pin text; never published 0.7.1 alone.
- Prefer one npm cut as **0.7.2** with dual changelog sections.

#### Explicit non-actions (founder hard rule)

- No `npm publish`
- No `git push` / tag / deploy
- No commit unless founder asks after review

---

## 2. Working-tree snapshot (CLI) as of end of session

**Branch:** `main` (was ahead of `origin/main` by 2 *before* this work — reconcile on push)

**Modified (expected):**  
`README.md`, `action.mjs`, `brief.mjs`, `checks-runner.mjs`, `checks.mjs`,
`deploy.mjs`, `fanout.mjs`, `handoff.mjs`, `index.mjs`, `init.mjs`, `ledger.mjs`,
`mcp.mjs`, `overviews.mjs`, `package.json`, `report.mjs`, `switch.mjs`,
`tests/run.mjs`, `util.mjs`

**Untracked (expected):**  
`ROADMAP.md`, `docs/SESSION-HANDOFF-0.7.1.md`, `docs/SESSION-HANDOFF-0.7.2.md`,
`docs/REVIEW-0.7.1.md`, `docs/RELEASE-0.7.2-REVIEW.md`, `docs/COMMIT-MSG-0.7.2.txt`,
`docs/sessions/2026-07-19-v1-SESSION.md` (this file)

**Verify:**

```powershell
Set-Location C:\Users\ben\projects\getadvantage-cli
npm test
# expect: 34/34
node index.mjs --version
# expect: 0.7.2
```

---

## 3. Product map (cross-repo, as discussed)

| Product | Path | Domain / notes |
|---------|------|----------------|
| getAdvantage | `projects/getadvantage` | getadvantage.app · EN SaaS / Trust thesis · messy git |
| plus×plus | `projects/plusxplus` | plusxplus.de · DE sales + `/sales` Operator · cleaner git |
| CLI | `projects/getadvantage-cli` | npm `getadvantage` · 0.7.2 RC uncommitted |
| Studio / control | `projects/advantagestudio` | Mission control hub (mentioned in HANDOFF; not deep-dived this session) |

Shared between getAdvantage ↔ plus×plus: **Neon + Stripe only**. Crons on getAdvantage.

---

## 4. Decisions / preferences from founder this session

1. **Tokyo Night** theme for Grok TUI — applied in `~/.grok/config.toml`.
2. **Worktree/doc mess:** deprioritize cleanup; work in the right repo and move on.
3. **CLI:** no publish/push/tag/deploy without explicit founder OK.
4. **CLI scope:** finish 0.7.2 top-down (brief first, then map+check + polish) at high quality.
5. **Release:** want commit message + release notes + full review pack for another session.
6. **0.7.1** must be evaluated on its own (not only buried inside 0.7.2 pack) — done in `REVIEW-0.7.1.md`.
7. **Conversation export:** TUI `/export` failed → durable session logs under `docs/sessions/`.

---

## 5. Open founder decisions (still open)

1. Commit + tag + publish 0.7.2? (after review session ship/no-ship)
2. Drop `ship-safe` bin alias entirely, or keep with warning only? (docs already warn)
3. npm provenance from GHA on publish?
4. getAdvantage git: pull/rebase the behind-83 / ahead-2 situation when working there again
5. plus×plus vs getAdvantage: which product next for feature work?

---

## 6. Suggested next-session menus

### A — Release review / cut (recommended next)

```
Read docs/sessions/2026-07-19-v1-SESSION.md
Read docs/REVIEW-0.7.1.md (SHIP verdict — reconfirm or challenge)
Read docs/RELEASE-0.7.2-REVIEW.md
npm test
Produce final ship/no-ship. If ship and founder says so: commit with
docs/COMMIT-MSG-0.7.2.txt, then tag/publish only if founder asks.
```

### B — Continue product engineering

- **CLI 0.8:** policy/baseline, SARIF, Action, client-bundle keys (`ROADMAP.md`)
- **plus×plus `/sales`:** outreach / Marc loop
- **getAdvantage:** Trust/Ship Gate docs vs code — founder direction first

### C — Hygiene (only if blocking)

- Prune stale `getadvantage-*` worktrees
- Reconcile getadvantage `main` vs `origin/main`

---

## 7. Key file index for cold start

```
getadvantage-cli/
  package.json                 # version 0.7.2
  ROADMAP.md
  docs/
    sessions/
      2026-07-19-v1-SESSION.md   ← YOU ARE HERE
    REVIEW-0.7.1.md
    RELEASE-0.7.2-REVIEW.md
    COMMIT-MSG-0.7.2.txt
    SESSION-HANDOFF-0.7.1.md
    SESSION-HANDOFF-0.7.2.md
  *.mjs + tests/run.mjs        # product code (uncommitted)

~/.grok/config.toml            # theme = tokyonight
~/.grok/tokyo-night-preview.html
```

---

## 8. Naming convention for future session logs

| Pattern | Example |
|---------|---------|
| File | `docs/sessions/YYYY-MM-DD-vN-SESSION.md` |
| First export of a day | `…-v1-SESSION.md` |
| Same day, more work, re-export | `…-v2-SESSION.md` |
| New day | new date, start again at `v1` |

Put the log in the **primary repo of that session’s work**. Cross-link other
products inside the log (as §3 does here).

---

## 9. One-paragraph summary (paste into any agent)

On 2026-07-19 we oriented on getAdvantage (AEO SaaS, messy git, strategy drift)
and plus×plus (own repo, DE sales + Operator `/sales`, healthier git), set Grok
TUI to Tokyo Night, then completed getAdvantage CLI **0.7.1 trust fixes review
(SHIP)** and **0.7.2 honesty/branding/polish implementation (34/34 tests,
version 0.7.2, uncommitted)**. Review pack, commit message, and 0.7.1 review
live under `getadvantage-cli/docs/`. Nothing was published or pushed. Next:
independent release review, then founder-gated commit/tag/publish.

---

*End of session log `2026-07-19-v1`.*
