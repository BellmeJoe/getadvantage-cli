# Active lanes (multi-agent lock board)

**Owner fills this before starting an agent session.** One line per open lane.  
Empty board = nobody claims exclusive work — first writer updates this.

| Lane | Owner (tool/person) | Path / concern | Status | Updated |
|------|---------------------|----------------|--------|---------|
| `0.8.3-policy-safety-repair` | Grok Build | `policy.mjs`, `util.mjs`, `checks.mjs` index-blob policy, PEM full-block + incomplete, dirty-tree config carve-out, tests, README, package, social pack | **LIVE 0.8.3** · `done` · commit `4b8fc76` · Actions [29754507154](https://github.com/BellmeJoe/getadvantage-cli/actions/runs/29754507154) success · `npm view` **0.8.3** · cold `npx getadvantage@0.8.3 --version` 0.8.3 · clean GO · secret NO-GO · tag `v0.8.3` · [GitHub Release](https://github.com/BellmeJoe/getadvantage-cli/releases/tag/v0.8.3) | 2026-07-20 |
| `0.8.4-github-native-sarif` | Grok Build | `sarif.mjs`, `action.mjs`, `checks.mjs` findings, `index.mjs` `--sarif`, tests, README, ROADMAP, social pack, NORTHSTAR | **LIVE 0.8.4** · `done` · commit `cd2a34d` · Actions [29761922904](https://github.com/BellmeJoe/getadvantage-cli/actions/runs/29761922904) success · `npm view` **0.8.4** · cold published tarball: version 0.8.4 · clean GO + SARIF · secret NO-GO · fixture secret absent · workflow pins v4/v6 · tag `v0.8.4` · [GitHub Release](https://github.com/BellmeJoe/getadvantage-cli/releases/tag/v0.8.4) · P3 carry: fork-PR upload verify; SARIF leftover nag; `--sarif` overwrite; `--report` path-credential | 2026-07-20 |
| `0.9.0-first-party-action-pr-summary` | Grok Build | `0.9.0-publish-self-gate-fixture`: versioned clean `fixtures/publish-self-gate` + publish workflow materialize + `uses: ./` `working-directory`; product-root still NO-GO on `tests/run.mjs` hostiles | **LIVE_VERIFIED 0.9.0** · `done` · release commit `6e982ca` · Actions [29862745770](https://github.com/BellmeJoe/getadvantage-cli/actions/runs/29862745770) Materialize + `uses: ./` fixture gate + npm publish + gitHead verify **success** (tag/Release step failed runner git-identity; leader repaired) · `npm view` **0.9.0** · registry `gitHead` = `6e982ca` · tag `v0.9.0` + floating `v1` + [GitHub Release](https://github.com/BellmeJoe/getadvantage-cli/releases/tag/v0.9.0) · cold pack: version 0.9.0 · clean GO · secret NO-GO · SARIF full+fragment redaction · rollback `0.8.4`/`v0.8.4` intact · evening Claude **REVIEW_GO** manifest `c06f7b7d…` | 2026-07-21 |
| `0.9.x-client-bundle-secret-exposure` | Grok Build | Scan committed client build assets, especially `.next/static/**` and Vite `dist/**`, without treating `NEXT_PUBLIC_*` / `VITE_*` prefixes as proof that a private value is safe | **LIVE_VERIFIED 0.9.1** · `done` · product release commit `7952124` · published source `gitHead` `c34e4c2` (version-pin test fix) · Actions [29906298164](https://github.com/BellmeJoe/getadvantage-cli/actions/runs/29906298164) tests+evidence+fixture gate+npm publish+gitHead **success** (tag/Release: GITHUB_TOKEN rejected tag push for workflow permission; leader repaired) · `npm view` **0.9.1** · tags `v0.9.1` + floating `v1` + [GitHub Release](https://github.com/BellmeJoe/getadvantage-cli/releases/tag/v0.9.1) · cold published: `.next/static` NO-GO redacted · cache/server honest GO · nested monorepo NO-GO · VITE_-named key NO-GO · public config GO · SARIF full-key absent · rollback `0.9.0`/`v0.9.0` + `0.8.4`/`v0.8.4` intact · audit REVIEW_GO manifest `6dae513e…` | 2026-07-22 |
| `0.9.x-vite-react-supabase-map` | Grok Build | Same-lane: Vite/React/Supabase client orientation + dogfood reliability (quiet missing-Intent; map ignores test/fixture + string-embedded routes; hostiles stay NO-GO) | **LIVE_VERIFIED 0.10.1** · `done` · product fingerprint `4001a58` · release `7592004` · Actions [30168966719](https://github.com/BellmeJoe/getadvantage-cli/actions/runs/30168966719) success · `npm view` **0.10.1** · registry `gitHead` = `7592004` · tags `v0.10.1` + floating `v1` + [GitHub Release](https://github.com/BellmeJoe/getadvantage-cli/releases/tag/v0.10.1) · cold `npx getadvantage@0.10.1` version 0.10.1 · map `clientOrientation` on Vite+React+Supabase fixture · rollback **0.10.0** / `v0.10.0` / `705986c` intact | 2026-07-25 |
| `0.10.0-intent-contract-gate` | Grok Build | Local Intent Contract: human goal bound to enforceable repo-change envelope; immutable `baselineCommit` + dedicated freeze-blob trust; committed+dirty coverage; `receiptHash`; nested-git/gitlink fail-closed; CLI/JSON/MCP/Action surfaces | **LIVE_VERIFIED 0.10.0** · `done` · release commit `705986c` · Actions [30021314088](https://github.com/BellmeJoe/getadvantage-cli/actions/runs/30021314088) success · `npm view` **0.10.0** · registry `gitHead` = `705986c` · tags `v0.10.0` + floating `v1` + [GitHub Release](https://github.com/BellmeJoe/getadvantage-cli/releases/tag/v0.10.0) · cold published: version 0.10.0 · intent init+check GO · outside-allow NO-GO · `npm test` 96/96 · evidence 8/8 · rollback `0.9.1`/`v0.9.1` + `0.9.0`/`v0.9.0` intact | 2026-07-23 |
| `0.9.1-release-tagger-gate-repair` | Grok Build | Bounded current-main release-pipeline gate repair: reconcile the repository-local tagger-identity contract/test order without weakening publish, source-identity, tag, Release, or rollback safeguards | **done** · Grok 4.5 HEAVY best-of-5 selected candidate 4 · `publish.yml` name-then-email local tagger + `validatePublishWorkflowContract` regression · `npm test` **71/71** · `npm run evidence` **8/8** · no version/publish/tag/LIVE claim change · Vite lane deferred by founder priority | 2026-07-22 |

| `0.10.x-supabase-rls-ungated-mutations` | Grok Build | Supabase RLS / ungated mutations static policy-state (class-level lineage + prior instance repairs). Code left in tree, committed, unreleased — no revert. | **PARKED_INSUFFICIENT** @ product fingerprint `d246171`. Eight consecutive independent `REVIEW_NO_GO` verdicts at `fdb81a4`, `c0bf5dd`, `31c9c90`, `ba03158`, `7f41d06`, `3ef84cf`, `786736d`, `d246171` (same fail-open shape each time). Adversarial Postgres DDL is unbounded: six instance-level repairs plus one class-level lineage repair each closed a single construct or family with no convergence argument. Zero real-repository RLS evidence after fifteen cycles (both dogfoods skip — no Supabase migration SQL or Edge Function sources); every proof is a synthetic fixture. Meets `docs/NORTHSTAR.md:125` kill criteria ("remains too broad for hostile independent review"). Live remains **0.10.1** / `7592004`. | 2026-07-29 |
| `0.11.x-intent-in-merge-train-gate` | Grok Build | `checks-runner.mjs` `gateTree` — wire `checkIntent` into fan-in merge-train combined-tree gate (parity with `runChecks`); freeze trust history-first; outside-allow / untrusted → quarantine + rollback; no contract → clean omit | **LIVE_VERIFIED 0.11.0** · `done` · product fingerprint `cef3491` · release commit `96fb5f8` · Actions [30549828041](https://github.com/BellmeJoe/getadvantage-cli/actions/runs/30549828041) success · `npm view` **0.11.0** · registry `gitHead` = `96fb5f8` · tags `v0.11.0` + floating `v1` + [GitHub Release](https://github.com/BellmeJoe/getadvantage-cli/releases/tag/v0.11.0) · cold published: clean GO · no Supabase/RLS line · rollback **0.10.1** / `v0.10.1` / `7592004` intact · *scope verified; semantic correctness not proven* | 2026-07-30 |
| `0.11.x-unexpose-parked-rls` | Grok Build | Stop exposing parked `checkSupabaseRls` on `runChecks` + `gateTree` (implementation stays in-tree); same 0.11 release train as D1 — not a second product capability | **LIVE_VERIFIED 0.11.0** · `done` · product fingerprint `cef3491` · release `96fb5f8` · RLS remains **PARKED_INSUFFICIENT** (open P1/P2); unexposed on all shipped surfaces; not advertised · Actions [30549828041](https://github.com/BellmeJoe/getadvantage-cli/actions/runs/30549828041) success · same npm/tags/Release as D1 | 2026-07-30 |
| `0.11.x-paste-ready-remediation` | Grok Build | **B1** — on secret-scan NO-GO, each blocking secret finding names the smallest safe next edit; secret scanner emits a **pattern-aware** paste-ready `.getadvantage/config.json` `secrets.ignore` snippet (auth-id hashes for unique patterns; paths-only for non-unique). Suppressed ≠ invisible: always printed as disclosed. Additive only — no verdict or exit-code change. `checks.mjs` emission + `policy.mjs` read paths + `tests/run.mjs` | **LIVE_VERIFIED 0.11.1** · `done` · product fingerprint **`25f0e54`** · release commit **`66ffb9b`** · Actions [30637594931](https://github.com/BellmeJoe/getadvantage-cli/actions/runs/30637594931) success · `npm view` **0.11.1** · registry `gitHead` = `66ffb9b` · tags `v0.11.1` + floating `v1` + [GitHub Release](https://github.com/BellmeJoe/getadvantage-cli/releases/tag/v0.11.1) · cold published: clean GO (~6.1s, exit 0, no Supabase/RLS line) · secret fixture NO-GO exit 1 with paste-ready `Smallest safe next edit` + auth-id `hashes` snippet · rollback **0.11.0** / `v0.11.0` / `96fb5f8` | 2026-07-31 |
| `0.12.x-invisible-mode` | Grok Build | **B2** — one command (`getadvantage init --claude-code`) installs the gate as an **automatic hook**, so every agent session is gated by default; Intent Contract auto-captured; the hook writes an in-repo **proof receipt** so week-two reuse is countable by GitHub code search with **zero telemetry**. Split modules: `invisible.mjs` + `invisible-hook.mjs` + `init.mjs` wire + `tests/run.mjs`. Cursor = detect-and-refuse | **LIVE_VERIFIED 0.12.0** · `done` · product fingerprint **`fe9e2ad`** · release commit **`b8be10c`** · Actions [30790630836](https://github.com/BellmeJoe/getadvantage-cli/actions/runs/30790630836) success · `npm view` **0.12.0** · registry `gitHead` = `b8be10c` · tags `v0.12.0` + floating `v1` + [GitHub Release](https://github.com/BellmeJoe/getadvantage-cli/releases/tag/v0.12.0) · cold published: clean GO (~5.3s, exit 0, no Supabase/RLS line, zero raw git/fatal noise) · activation cold→`init --claude-code`→first gate **~11.3s** · suite **218/218** (release-era) · evidence **8/8** · pack **34 files** · post-release P3 symlink mode `120000` coverage closed on sibling lane · rollback **0.11.1** / `v0.11.1` / `66ffb9b` · *scope verified; semantic correctness not proven* · never re-expose RLS · never claim Cursor or adoption | 2026-08-03 |

| `0.12.x-symlink-escape-regression-coverage` | Grok Build | **P3 from `reviews/2026-08-02-claude-cli-audit-evening.md`** — symlink mode `120000` escape had zero automated coverage while three sibling structural detectors (nested `.git`, gitlink `160000`, absolute/`..`) all had hostile tests. Runtime protection already correct; this was **coverage, not repair**. `tests/run.mjs` **only** | **done** · commit **`9318271`** · product fingerprint **`5f0422d`** (`tests/run.mjs` blob; base `ae0f08b`) · independent audit **`REVIEW_GO` (lane-scoped)** `agent-ops/reviews/2026-08-03-claude-cli-audit-midday.md` at exact fingerprint · mutation-check: neuter both `mode === "120000"` branches in `detectGitlinkOrSymlinkModes` → both new scenarios fail (`0 !== 1`, `0 !== 2`) · leader re-run suite **220/220** · evidence **8/8** · pack **34 files** · live **0.12.0** / `b8be10c` unaffected · no product-code / version / tag / publish / `SOCIAL-MEDIA-PACK` · backup only `backup/2026-08-03-symlink-coverage` · session `docs/sessions/2026-08-03-0.12.x-symlink-escape-regression-coverage-CLOSED.md` | 2026-08-03 |
| `0.12.x-remediation-output-density` | Grok Build | **Bet 4, promoted 2026-08-03 16:45** — presentation-only remediation density. Since `0.12.0` shipped invisible mode the hook fires the gate automatically, so this wall of text is now the **first thing a new installer sees**. Measured baseline (this routine, `node index.mjs check`, stdout+stderr, `wc -l` / `grep -n "^Verdict"`): CLI repo `9a97120` **109 lines, verdict header 106/109**; site repo **157 lines, verdict header 154/157**. Deduplicate repeated boilerplate, merge per-finding snippets into one paste-ready block, keep the verdict readable without scrolling. **Hard constraints:** no verdict change, no exit-code change, every finding still individually named with its own auth id, suppression still printed and disclosed, additive-to-neutral only. `checks.mjs` / `checks-runner.mjs` / `index.mjs` presentation paths + `tests/run.mjs` | **done** · independent audit **`REVIEW_GO` (lane-scoped)** at exact fingerprint `9b65aec` (docs pins `a17c0a8`, `7dbb510`), **0 P1 / 0 P2 / 0 P3** — `agent-ops/reviews/2026-08-04-claude-cli-audit-am.md`; auditor independently reproduced the 5-secret hostile path in a throwaway repo outside the fixture tree, re-ran suite **221/221**, evidence **8/8**, pack **34 files / shasum `ac724ef7…`** (exact match), and re-measured density a fifth time (CLI 56/53, site 143/140 — match). Explicitly **not release-eligible**: presentation-only, **rides the next release train** with `5f0422d`. Live unmoved at **0.12.0** / `b8be10c` · product fingerprint **`9b65aec`** · best-of-5 selected candidate 3 · **density re-measured on the committed tree by the dispatch routine (three runs, clean tree): CLI 109 → 56 lines, verdict header 106 → 53; site (read-only) 157 → 143 stdout-only, verdict 154 → 140.** The leader's own row first read CLI 59/56 and site 151/148 — taken from an intermediate pre-commit state and **corrected here**; the committed code is better than the number it claimed, never worse. Method, so it stays comparable: `node index.mjs check`, `wc -l`, `grep -n "^Verdict"`, stdout-only for the site (its 1-line `.ship-safe` stderr notice makes the merged count 158 → 144) · suite **221/221** · evidence **8/8** · pack **34 files / shasum `ac724ef7…`** · `--json` `findings[].remediation` **byte-identical** and SARIF **byte-identical** to the pre-lane baseline (only human `extra` changes) · version **0.12.0** unmoved · live still **0.12.0** / `b8be10c`; rollback `0.11.1` / `v0.11.1` / `66ffb9b` · no push / tag / publish / LIVE claim | 2026-08-03 |

| `0.12.x-api-map-density` | Grok Build | **Promoted 2026-08-04 12:45** on measured dogfood friction, not a roadmap guess. On the real site repo `check` prints **143 lines** and the API-surface-map block is **lines 36–114 = 79 lines, 55% of the whole first screen**; the 60 route lines it prints inline are **byte-identical** to lines the already-shipped `getadvantage map` command prints, and `map` prints a strictly fuller 68. Since `0.12.0`'s hook fires the gate automatically this is the **first screen a new installer sees**. Keep the header counts, all 14 `⚠` ungated-mutating routes, and the guidance; drop only the inline `— full map —` dump and point at `getadvantage map` in one line. **Ship blocker:** shared `overviewApiSurface` — a hostile test must prove the dedicated `map` command still prints its full route output byte-identically. `overviews.mjs` presentation paths + `tests/run.mjs` only | **done** · independent audit **`REVIEW_GO` (lane-scoped)** at exact fingerprint `4b69b51` (docs pins `d987840`, `07389c5`), **0 P1 / 0 P2 / 0 P3** — `agent-ops/reviews/2026-08-04-claude-cli-audit-midday.md`. The auditor independently reproduced the hostile paths on a throwaway repo built outside the fixture tree, re-ran suite **225/225**, evidence **8/8**, pack **34 files / shasum `60d89430…`** (exact match), re-measured density on both repos (CLI 56/53, site 82/79 — match), and proved the ship blocker by hash: `map` from a temporary detached worktree at pre-lane `7dbb510` and from the candidate both produce 129 lines, sha256 `a99ab5db2df8…`, with the site repo's HEAD and porcelain sha256 unchanged before and after. Verdict is **explicitly not release-eligible**: presentation-only, **rides the next release train** with `5f0422d` and `9b65aec`. · product fingerprint **`4b69b51`** · Grok 4.5 HEAVY best-of-5 selected **candidate 4** (`forMap` default compact; `renderMap` full). Site (read-only `c3040b0`, stdout-only): **143 → 82 lines**, verdict header **140 → 79**; all **14** ⚠ named; pointer `Run getadvantage map…`; no `— full map —` on check. Site `map` human output **byte-identical** (129 lines). CLI: API lane zero-route (0 extra both sides); **56 lines / verdict 53 — unchanged from pre-lane**, corrected here by the dispatch routine from the leader's `58/55`, which was read while the `index.mjs` Windows EOL artifact was still dirty; re-measured on a **0-entry clean tree** (`node index.mjs check`, `wc -l`, `grep -n "^Verdict"`), exit 1 on the repo's own disclosed hostile fixtures, 1.47s, 0 bytes product stderr. Suite **225/225** (was 221 + 4 hostiles) · evidence **8/8** · pack **34 files** / shasum `60d89430…` · site `--json` `findings[]` **byte-identical** + SARIF **byte-identical**; CLI findings[] empty/identical (SARIF only differed mid-lane on dirty-tree *file count* while product files were uncommitted — not a findings payload change). **All of the above independently re-run by the dispatch routine, not accepted from the leader:** suite 225/225, evidence 8/8, pack shasum `60d894303c0687d93dc6a61107137d0500d4fc88` exact match, site `map` pre-lane (`7dbb510`, temp worktree) vs candidate both sha256 **`a99ab5db2df8…`** — the ship blocker holds byte-for-byte; site SARIF both sha256 `64eaeff847bf…`; `checks[]` minus the human `extra` array byte-identical, per-check `findings[]` byte-identical, `extra` 10,975 → 8,305 bytes; site unmutated (`c3040b0`, porcelain sha256 `70be9aeb…` identical before and after). **Acceptance note, stated honestly: 82 lines is 2 over the `≤ ~80` soft target**, because candidate 4 deliberately removed the 20-route cap so every `⚠` route is named — a correctness gain paid for in two lines. Version **0.12.0** unmoved · live still **0.12.0** / `b8be10c`; rollback `0.11.1` / `v0.11.1` / `66ffb9b` · **no push / tag / publish / LIVE claim / SOCIAL-MEDIA-PACK** | 2026-08-04 |

| `0.12.1-release-train` | Grok Build | **Not a code lane — a release decision.** Three audited lanes on top of live `0.12.0`: `5f0422d` (symlink-escape regression coverage, test-only), `9b65aec` (remediation output density, presentation-only), `4b69b51` (API-map density, presentation-only). Patch only — no new check, no changed verdict/exit, no machine-surface change. Measured: **CLI `check` 109 → 56 lines**; **site `check` 143 → 82 lines** | **LIVE_VERIFIED 0.12.1** · `done` · product fingerprint **`4b69b51`** · release commit **`e2f8665`** · train-scoped **REVIEW_GO (release-eligible)** 0 P1/P2/P3 · Actions [30980794656](https://github.com/BellmeJoe/getadvantage-cli/actions/runs/30980794656) success · `npm view` **0.12.1** · registry `gitHead` = `e2f8665` · tags `v0.12.1` + floating `v1` + [GitHub Release](https://github.com/BellmeJoe/getadvantage-cli/releases/tag/v0.12.1) · cold published: clean GO **~5.0s** exit 0, no Supabase/RLS line, zero raw git/fatal noise · HEAVY four-specialist GO (product/architecture patch; hostile security RLS still unexposed; test/cold-path live 8/8 + pack shasum; owner-truth) · suite/evidence/Action self-gate green in CI · rollback **0.12.1** / `v0.12.1` / `e2f8665` intact (post-0.12.2 ship target; was 0.11.1 at train time) · never advertise RLS · never claim Cursor or adoption | 2026-08-05 |

| `0.12.x-pr-summary-findings-density` | Grok Build | `action/summary.mjs` + `tests/run.mjs` (presentation only) — PR/job summary lists every finding (not only the first) with paste-ready remediation, file:line locations, blocking-first order | **LIVE_VERIFIED 0.12.2** · `done` · product fingerprint **`653a693`** · release commit **`1e451ac`** · independent audit **REVIEW_GO (lane-scoped)** 0 P1/P2 · HEAVY four-specialist panel GO (product/architecture presentation-only patch; hostile security marker not forgeable, BODY_CHAR_CAP holds, RLS unexposed; test/cold-path 226/226 + evidence 8/8 + pack 34 files / 325.6 kB; owner-truth P3-2 rollback repaired) · closes four defects: multi-finding table rows, paste-ready remediation in PR summary, line numbers as `file:line`, blocking-first fail-before-warn · caps: `FINDING_CAP 20`, `FINDINGS_PER_CHECK 8`, `REMEDIATION_FINDING_CAP 5`, `REMEDIATION_LINES_PER_FINDING 8`, `REMEDIATION_CHARS_PER_FINDING 480`, `BODY_CHAR_CAP 12000`, `CHECK_CAP 12` · suite **226/226** · evidence **8/8** · pack **34 files / 325.6 kB** · Actions [31153799756](https://github.com/BellmeJoe/getadvantage-cli/actions/runs/31153799756) success · `npm view` **0.12.2** · registry `gitHead` = `1e451ac` · tags `v0.12.2` + floating `v1` + [GitHub Release](https://github.com/BellmeJoe/getadvantage-cli/releases/tag/v0.12.2) · cold published: clean GO **~5.0s** exit 0, no Supabase/RLS line, zero raw git/fatal noise · rollback **0.12.1** / `v0.12.1` / `e2f8665` · never advertise RLS · never claim Cursor or adoption | 2026-08-07 |

| `0.12.x-report-transparency-mode` | Grok Build | **Founder queue item 5** — `--report-dry-run` transparency mode: builds exact `buildRunBody()` body, prints endpoint + byte size + key-resolved-source (never value), **zero network**. Transparency gap closed, not a leak. Product: `report.mjs` + `index.mjs` + `README.md` + `tests/run.mjs`; activation wall-clock rescope is test-only on top | **LIVE_VERIFIED 0.13.0** · `done` · product fingerprint **`cdb923c`** (feat `472ca83` + test rescope `cdb923c`) · independent audit **`REVIEW_GO (release-eligible)`** 0 P1/P2 (`agent-ops/reviews/2026-08-12-claude-cli-audit-midday.md`) · HEAVY four-specialist panel **GO** (product/architecture: minor 0.13.0 correct; hostile security: dry-run 0 requests / plain `--report` exactly 1 / key never printed / RLS unwired; test/cold-path: suite **230/230** ~100.7 min, evidence **8/8**, pack pre-bump **34 files / 330.8 kB / shasum `bbaab154…`**, 4/4 dry-run hostiles; adoption/owner-truth: no false claims) · **P3s disclosed not absorbed:** (a) `MAX_BODY_BYTES` not exported; (b) `--json` value drift wider — `startLine`, `primaryLocationLineHash`, `remediation[0]`, `generatedAt` (no field add/remove/rename/reorder); (c) dry-run banner "Never sent … API key" over-broad for live `--report` (key leaves as Authorization bearer) — pack copy does not claim key never transmitted; follow-up lane for banner reword; (d) zero-request asserts at child exit / key-absence exact-substring only · **FU-1** carried: rescope left tail limbs without wall-clock budget · suite **230/230** · evidence **8/8** · product-surface diff `cdb923c..release` empty for `report.mjs index.mjs tests/run.mjs README.md` · rollback **0.12.2** / `v0.12.2` / `1e451ac` · never advertise RLS · never claim Cursor or adoption · Actions [31679618395](https://github.com/BellmeJoe/getadvantage-cli/actions/runs/31679618395) success · `npm view` **0.13.0** · registry `gitHead` = `bdc8b04` · tags `v0.13.0` + floating `v1` + [GitHub Release](https://github.com/BellmeJoe/getadvantage-cli/releases/tag/v0.13.0) · cold published: clean GO **~38.6s** exit 0, 20 lines, no Supabase/RLS line, zero raw git/fatal noise · release commit **`bdc8b04`** | 2026-08-13 |

| `0.13.x-collapsed-remediation-fileline-restore` | Grok Build | **Bet 1** (score 3.30, effort 1) — quality review `reviews/2026-08-07-0938.md` **F1**. Human terminal secret rows + collapsed remediation restore `file:line` when `startLine` is a number; bare path when region unknown (no `:undefined`). Presentation-only — no verdict/exit/`--json` field shape/SARIF change. Folded **FU-1**: invisible cold-install tail limbs (secret-scan + bypass + re-init) get a separate wall-clock budget **`<180s`** (not charged against the 60s activation contract). Does **not** fold P3-c banner reword (bet 2, separate lane). Product: `checks.mjs` (`formatHitPath`) + `tests/run.mjs` (density pin, two-file two-finding, no-startLine path-only, FU-1 budget). | **done** · independent audit **`REVIEW_GO (lane-scoped)`** at exact fingerprint **`ce5c182`**, **0 P1 / 0 P2 / 0 P3** — `agent-ops/reviews/2026-08-13-claude-cli-audit-evening.md`. Auditor independently reproduced both hostile paths in throwaway repos built outside the fixture tree (two-file two-finding → both rows carry `file:line`; 5 MB partial-scan file → bare path, no `:undefined`, no dangling colon), re-ran suite **232/232** (~62 min on that host), evidence **8/8 GREEN**, pack **34 files / 332.4 kB / shasum `bd93fb7f0019628ed29f83ed274f4d28d87b22c0`** (exact match), and traced `startLine`'s origin at `checks.mjs:505-517` to prove the `typeof === "number"` gate has no reachable `:undefined`/`:0` edge case. Candidate stable across the audit (diff sha256 `c911d142…` identical at acquisition and finalization). **Explicitly NOT release-eligible** — presentation-only, **rides the next release train**, same shape as the three prior 0.12.x presentation lanes. Product fingerprint **`ce5c182`** · Grok 4.5 HEAVY · second dispatch after first hit 40-turn ceiling with candidate 1 already applied · selected **candidate 1** (first dispatch best-of-5) · FU-1 budget shipped: **`tailElapsed < 180`** · no version/tag/publish/Release/`SOCIAL-MEDIA-PACK` · live unmoved **0.13.0** / `bdc8b04`; rollback **0.12.2** / `v0.12.2` / `1e451ac` · never re-expose RLS · never claim Cursor or adoption · session `docs/sessions/2026-08-13-0.13.x-collapsed-remediation-fileline-restore-SESSION.md` | 2026-08-14 |

| `0.13.x-dry-run-summary-density-and-key-scoping` | Grok Build | **Bet 2** (score 1.65, effort 2) — promoted 2026-08-14 08:45 on the lane-scoped `REVIEW_GO` that closed bet 1. Three items on `report.mjs` presentation paths + `tests/run.mjs` **only**: (1) collapse the dry-run block's duplicate remediation (31 of its 74 lines are byte-identical repeats of text plain `check` printed directly above; only 4 lines carry the transparency payload the flag exists for); (2) scope the P3-c banner precisely — `Never sent: … API key …` is over-broad because a live `--report` sends the key as an `Authorization` bearer, so distinguish report **body** from **transport**; (3) **correctness repair** — `report.mjs:298`'s `f.remediation.slice(0, 8)` truncates every paste-ready JSON snippet three braces short of valid, five times, under a header reading `Paste-ready` (live defect in shipped `0.13.0`, found by the 2026-08-13 20:45 REFILLING pass). Fix (3) **by collapsing**, never by raising the slice. **Ship blockers:** brace-balance over the block (`"secrets": {` count == `"ignore": {` count; today 5 vs 0), preview byte-identical to `buildRunBody()`, `--json`/SARIF byte-identical, key value absent from stdout **and** stderr, and `file:line` finding rows stay **≥ 5** so bet 1 is not silently undone. | **done** · independent audit **`REVIEW_GO (lane-scoped)`** at exact product fingerprint **`ec92722`** (docs pin `52b47b6`), **0 P1 / 0 P2 / 0 P3** — `agent-ops/reviews/2026-08-14-claude-cli-audit-am.md`. The auditor independently re-ran the **full suite to completion: 234/234** (= bet 1's audited 232 + this lane's 2 new scenarios; arithmetic exact, no scenario drift) after one inconclusive `SUITE_EXIT:127` attempt it **disclosed rather than discarded**; evidence **8/8 GREEN**; reproduced all five ship blockers on a throwaway fixture built outside every product tree — brace balance **0/0** in the dry-run block, `--json`/SARIF **byte-identical** to the `ce5c182` pre-lane baseline, key value **absent** from stdout *and* stderr under a live-shaped 33-char fixture key, **5** `file:line` rows (bet 1 intact), report body `JSON.parse`s cleanly. It also graded the routed question: the shipped-`0.13.0` `report.mjs:298` slice-truncation defect is **P2**, confirmed by `JSON.parse` throwing (not substring match), and is **closed by this candidate's own fix**. Grok 4.5 HEAVY best-of-5 selected **candidate 1**; all five attempts met the density targets (convergence, not a lucky candidate) · **Clean-tree re-measurement by the dispatch routine, 2026-08-14 12:45 — correcting this row's own earlier numbers, which were read on a dirty tree:** dry-run total **130 → 92** (not 132 → 95), `"secrets": {` / `"ignore": {` across the whole dry-run output **6/1 → 1/1** (the surviving pair is the one complete, correctly-closed snippet plain `check` prints above), plain `check` **56 lines / verdict header 53 / 5 `file:line`**, 0 bytes product stderr, exit 1 · **The 08:45 record's flagged "58 → 59 line regression against a hard constraint" is resolved and was a measurement artifact, not a deviation:** measured in a detached clean worktree at pre-lane `ce5c182` versus the candidate, plain `check` is **56/53 on both**, and `diff` of the two outputs is **empty apart from three fixture line numbers** (`4428→4578`, `8543→8693`, `4129→4279`) that must move because the lane added 154 lines to `tests/run.mjs` above them. The constraint holds exactly · pack **34 files / 334.1 kB / shasum `217df92aaaacc8729eeefd6fccea3867c6db1a2a`** (re-verified, exact match) · **Explicitly NOT release-eligible** — presentation + correctness repair, **rides the `0.13.1` release train** with bet 1 · version **0.13.0** unmoved · live still **0.13.0** / `bdc8b04`; rollback **0.12.2** / `v0.12.2` / `1e451ac` · no version bump / tag / publish / LIVE claim / `SOCIAL-MEDIA-PACK` edit · **pushed to `origin/main` 2026-08-14 12:45** under the reviewed-commit push authority (no version bump, no tag) · never re-expose RLS · never claim Cursor or adoption · session `docs/sessions/2026-08-14-0.13.x-dry-run-summary-density-and-key-scoping-SESSION.md` | 2026-08-14 |

| `0.13.1-release-train` | Grok Build | **Not a code lane — a release decision.** Two independently audited lanes stacked on live `0.13.0`: **`ce5c182`** (bet 1, collapsed-remediation `file:line` restore — `checks.mjs` presentation) and **`ec92722`** (bet 2, dry-run summary density + key-banner scoping + the `report.mjs:298` paste-ready correctness repair — `report.mjs` presentation). Patch only: **no new check, no changed verdict, no changed exit code, no machine-surface change** (`--json`/SARIF byte-identical to the pre-lane baseline, independently re-verified by the 08-14 AM audit). **The train is defined by its two product fingerprints, not by the docs tip** — docs-only commits on top do not change what ships. Tip of `main` was `52b47b6` when the AM audit scored it and **`85ec2c3`** after this cycle's board update; `git diff --stat ec92722..85ec2c3` is `docs/ACTIVE-LANES.md` alone, so the audited product surface is byte-identical at both | **LIVE_VERIFIED 0.13.1** · `done` · product fingerprints **`ce5c182`** + **`ec92722`** · release commit **`e5b06f3`** · train-scoped **`REVIEW_GO (release-eligible)`** 0 P1/P2/P3 (`agent-ops/reviews/2026-08-14-claude-cli-audit-midday.md`) · HEAVY four-specialist panel GO (owner-truth after README body/transport fix) · Actions [31808631124](https://github.com/BellmeJoe/getadvantage-cli/actions/runs/31808631124) success · `npm view` **0.13.1** · registry `gitHead` = `e5b06f3` · tags `v0.13.1` + floating `v1` + [GitHub Release](https://github.com/BellmeJoe/getadvantage-cli/releases/tag/v0.13.1) · cold published: clean GO **~17.2s** exit 0, 25 lines, 0 RLS, 0 stderr · Both constituent lanes carry `REVIEW_GO (lane-scoped)` at their exact fingerprints with **0 P1 / 0 P2 / 0 P3** (`reviews/2026-08-13-claude-cli-audit-evening.md`, `reviews/2026-08-14-claude-cli-audit-am.md`), and **a lane-scoped GO authorizes closing its own lane and never a release** — so this train needs its **own train-scoped `REVIEW_GO (release-eligible)` at `52b47b6`** before any version bump, tag, `npm publish`, or GitHub Release. Same shape as `0.12.1-release-train`. **What it buys a user:** the paste-ready `secrets.ignore` snippet a `--report-dry-run` user copies is currently **invalid JSON** in shipped `0.13.0` (five truncated copies, `JSON.parse` throws) — this train is the fix reaching users. Gates already green at this fingerprint, each independently re-run rather than accepted: suite **234/234**, evidence **8/8 GREEN** (re-run again by the dispatch routine at 12:45), pack **34 files / 334.1 kB / shasum `217df92aaaacc8729eeefd6fccea3867c6db1a2a`**, dogfood triad clean (cold published `0.13.0` GO exit 0 / 24 lines; CLI repo exit 1 / 56 lines / verdict 53; site read-only exit 1 / 82 lines / verdict 79, provably unmutated). Rollback **0.12.2** / `v0.12.2` / `1e451ac`. Audit request written to `agent-ops/GROK-NEXT-TASK.md`. **No code lane may be dispatched while this train is the top board item** (one-lane rule; the `0.12.1` train set this precedent). Live **0.13.1** / `e5b06f3` · rollback **0.12.2** / `v0.12.2` / `1e451ac` · never advertise RLS · never claim Cursor or adoption | 2026-08-14 |

| `0.13.x-retained-team-detector` | Grok Build | **Measurement, not a product feature.** Ops observer for the north-star metric (retained external teams / week-two reuse). Reads public GitHub code-search hits for `.getadvantage/INVISIBLE-MODE.md` (`getAdvantage invisible-mode receipt`); excludes `BellmeJoe/*` and forks of them (exclusions listed with reason); classifies **install** (single ISO week) vs **retained** (≥2 distinct ISO weeks) vs **unknown-shape**; rate-limit/auth/network → **`UNKNOWN`** + non-zero exit, never `0`. **Hard scope:** `ops/retained-team-detector.mjs` + hostile scenarios in `tests/run.mjs` + this row only — no user-facing CLI/check/flag/verdict, no telemetry, no product network, no receipt format change, no version/tag/publish/`SOCIAL-MEDIA-PACK`. Injectable `fetch` so tests make zero real github.com calls. | **done** · independent audit **`REVIEW_GO (lane-scoped)`** at exact product fingerprint **`f885f31`** (docs pin `6092ce8`), **0 P1 / 0 P2**, one **P3 carried** — `agent-ops/reviews/2026-08-15-claude-cli-audit-am.md`. The auditor reproduced scope containment from scratch (`git diff --stat 2cbc94b..HEAD -- '*.mjs' 'action/' 'action.yml' ':!ops/' ':!tests/'` **empty**; no `retained-team-detector`/`retainedTeam` match in any user-facing surface), confirmed `ops/` is absent from `package.json` `files` so the tool ships to nobody, re-ran evidence **8/8 GREEN**, and read the full suite to completion in the durable log: **246/246**, `SUITE_EXIT:0`, **0** line-anchored FAIL. It also adjudicated the routed P2 **down to P3** after running the detector's exact live query against the real GitHub API read-only (`{"total_count":0,"incomplete_results":false,"items":[]}`) — the query is well-formed today and the `0` it returns is a real observed absence, so the missing self-check is a design nicety on an ops-only tool, not a defect that produces a wrong verdict. **`P3-1` carried as disclosed future work:** the tool has no internal positive-control assertion that its query still matches GitHub's search syntax, so a future API change could turn a broken query into a silent `0` with HTTP 200; today that control only exists because a human ran it. **Explicitly NOT release-eligible** — measurement infrastructure, no product surface, no version bump warranted. **Pushed to `origin/main` 2026-08-15** under the reviewed-commit push authority (no version bump, no tag), closing quality-review **F10** · Grok 4.5 HEAVY best-of-5 selected **candidate 5** · product fingerprint **`f885f31`** · tool `ops/retained-team-detector.mjs` (exports `isoWeekId` / `classifyReceipt` / `shouldExcludeRepo` / `runDetector`) · 12 hostile scenarios under `TEST_FILTER=retained-team` (single-week install ≠ retained; two-week retained; self + fork exclusion with reason; 403/401/network/missing-token → UNKNOWN; corrupt → unknown-shape; empty → plain `0` exit 0; mixed hits) · evidence **8/8 GREEN** · pack **34 files / 338.4 kB / shasum `30c86d954b3706f5dc69c8c327f2346da7702562`** · no-token smoke correctly **UNKNOWN** exit 1 · no version bump / tag / publish / `SOCIAL-MEDIA-PACK` · live remains **0.13.1** / `e5b06f3`; rollback **0.12.2** / `v0.12.2` / `1e451ac` · never advertise RLS · never claim Cursor or adoption | 2026-08-15 |

| `0.13.x-marketplace-listing-readiness` | Grok Build | **Bet 16 — free-shelf distribution** (`docs/NORTHSTAR.md:177`), Marketplace half only. Promoted 2026-08-15 on an empty board after `0.13.x-retained-team-detector` closed `done`. **Rationale, from measurement not roadmap:** the detector now returns a **validated zero** — the query is well-formed against the live GitHub API and nobody outside this machine runs the gate — so the binding constraint is **distribution, not capability**, which is why the week plan closed capability lanes for zero-user populations. This is the only remaining reach surface needing **no outbound message to any human**. Verified live this cycle: `github.com/marketplace/actions/{getadvantage-check,getadvantage,get-advantage-check}` → **404 at all three**; repo `public`, 1 star, 0 forks. **This lane does not publish the listing** — that needs one founder action no agent may take (Marketplace Developer Agreement + the publish checkbox the REST API cannot set on a bot-created Release). It makes that click **succeed first time and stay succeeding**, and makes the gap **visible** instead of silently permanent. **Hard scope:** `tests/run.mjs` (Marketplace publishing-contract scenarios under `TEST_FILTER=marketplace` — `name`/`description` non-empty, `branding.icon` in the allowed Feather set, `branding.color` in the exact eight, action file at repo root, valid `runs.using`; today greps show **zero** such assertions, so the metadata is valid by luck), `ops/action-release.mjs` (non-fatal release-time notice naming the remaining founder click), `docs/launch/MARKETPLACE-LISTING.md` (new), and this row. `action.yml` is **read-only input** — its metadata is already valid; the defect is that nothing guards it. **Forbidden:** any product `*.mjs`, `action/`, `action.yml`, `package.json`, version bump, tag, publish, Release, `SOCIAL-MEDIA-PACK` edit, new dependency, new CLI surface, telemetry. **Ship blockers:** mutation-proven tests (mutate `action.yml` in a scratch copy → scenario must fail, then restore); notice provably non-fatal; full `npm test` green + evidence **8/8**; pack stays exactly **34 files**; `git diff --stat ced87d7..HEAD -- '*.mjs' 'action/' 'action.yml' ':!ops/' ':!tests/'` **empty**; no first-screen regression (`node index.mjs check` stays **56 lines / verdict 53 / 5 `file:line` / 0 B stderr**). **Boundary rule:** if guarding the contract needs an `action.yml` edit or a YAML dependency, **stop and report** — do not widen scope. Brief: `agent-ops/GROK-NEXT-TASK.md`. | **done** — closed 2026-08-16 08:45 at product fingerprint **`9efe0c3`** (docs pin `05216ab`) on independent audit **`REVIEW_GO (lane-scoped)`**, **0 P1 / 0 P2 / 0 P3** — `agent-ops/reviews/2026-08-15-claude-cli-audit-evening.md` §10. The auditor independently re-probed all three Marketplace slugs (**200/404/404**, fifth independent confirmation), re-ran the full suite to completion **257/257** and evidence **8/8 GREEN**, verified scope containment from scratch (**1 file, +23/−13**; product-surface diff empty; pack byte-identical at **34 files / 344.9 kB / shasum `0685572c…`**), and confirmed the midday P2 independently closed — the "Live evidence" table now matches live reality by direct reproduction, the click path no longer instructs redoing a completed step, and a durable re-verify caveat is in place. **Explicitly NOT release-eligible** — docs-only content lane; a lane-scoped GO authorizes closing its own lane and nothing beyond it, so no version bump, tag, publish, or Release was authorized and none was taken. The doc content already lives on `main` at `05216ab`; **no push needed or made**. **Stop-loss closed at 1 of 3** (never reached the 3-NO_GO park-or-redesign threshold). · Lane history retained below as evidence: first candidate `5ccb102` took **`REVIEW_NO_GO`** (0 P1 / **1 P2**) from `agent-ops/reviews/2026-08-15-claude-cli-audit-midday.md`: the lane's own committed `docs/launch/MARKETPLACE-LISTING.md` asserted all three Marketplace slugs return 404 under a heading "Live evidence (this cycle — 2026-08-15)", but `github.com/marketplace/actions/getadvantage-check` returns a genuine **200** listing page (`<title>getAdvantage check · Actions · GitHub Marketplace · GitHub</title>`). The listing most plausibly went live between the commit (11:34:45) and the first probe (12:54) — the table may have been true when written, and went stale inside ~90 minutes. **Stop-loss counter: 1 of 3 same-shape NO_GOs.** Repair dispatched 2026-08-15 16:45 as Grok 4.5 HEAVY **best-of-5**, `--model grok-4.5 --reasoning-effort high`; **all five candidates independently re-probed the live URLs and converged on 200/404/404** (convergence, not a lucky candidate); coordinator selected **candidate 5** on founder clarity. Repair is **one file** — `docs/launch/MARKETPLACE-LISTING.md` only: live table corrected to 200/404/404 with probe method + UTC timestamp, an explicit **re-verify caveat** naming the ~90-minute staleness so the table is not read as a frozen fact, the click path reframed as **"for each future Release"** (the `v0.13.1` publish and the Developer Agreement have already succeeded — do not instruct repeating them), and the "listing absence is the distribution gap" line corrected. Reach-vs-adoption separation preserved verbatim; retained teams still stated as a **validated zero** next to the live URL — no overclaim in either direction. **Independently re-verified by the dispatch routine, not accepted from the leader:** `git diff --stat aaa35d3..9efe0c3` = **1 file, +23/−13**; the same diff over `*.mjs`/`action/`/`action.yml`/`package.json`/`tests/` is **empty**; evidence **8/8 GREEN**; pack **34 files / 344.9 kB / shasum `0685572c04b7769c4bed06232566bbb7d375ca7b`** — byte-identical to the pre-repair pack, which is itself the proof no shipped file moved (docs are absent from the tarball); first screen on a clean tree **56 lines / verdict header 53 / 5 `file:line` / 0 B product stderr / exit 1 / 1.56s** — invariant intact; live Marketplace state re-probed a **fourth** independent time (200/404/404). Dogfood triad all read-only and clean: cold published `0.13.1` **exit 0 / 5.05s / 24 lines / 0 B stderr**; CLI repo as above; site read-only **exit 1 / 82 lines / verdict 79 / 13.1s**, only the benign 147 B legacy `.ship-safe/` notice, site provably unmutated (`c3040b06a415`, 13 entries, porcelain sha256 `70be9aeb75a8fc71` identical before and after). · stops at REVIEW_PENDING · no push/tag/publish · suite at the repaired fingerprint `9efe0c3` re-run to completion by the dispatch routine: **257/257 scenarios passed, `SUITE_EXIT:0`, 0 FAIL** (`agent-ops/runs/suite-2026-08-15-1645-9efe0c3.log`) — arithmetic identical to the audit's own completed run at `5ccb102`, which is the expected result for a documentation-only change and the evidence that it is one · suite **257/257** · evidence **8/8 GREEN** · pack **34 files / 344.9 kB / shasum `0685572c04b7769c4bed06232566bbb7d375ca7b`** (docs/ + ops/ absent) · product-surface lane delta vs tip `e74d117` empty for `*.mjs`/`action/`/`action.yml` (note: `e74d117` already shortened `action.yml` description under Marketplace 125-char limit — orthogonal to this lane; live npm still **0.13.1** / `e5b06f3`) · first-screen on clean tree: **56 lines / verdict 53 / 5 file:line / 0 B stderr / exit 1** · 11 marketplace scenarios under `TEST_FILTER=marketplace` (mutation-proven: bad colour/icon, empty/overlong description, missing/placeholder name, invalid runs.using, nested root; notice non-fatal) · ops blob `2db350eb` · tests `abf1539c` · MARKETPLACE-LISTING `e56ea08b` · never re-expose RLS · never claim Cursor · **Marketplace listing is reach, never adoption** | 2026-08-16 |

| `0.13.x-readme-release-currency` | Grok Build | **Lane A — promoted 2026-08-16 08:45 on an empty board** after `0.13.x-marketplace-listing-readiness` closed `done`. **From measured dogfood, not roadmap:** the live Marketplace listing (`github.com/marketplace/actions/getadvantage-check`, 200) renders the repository README, and `README.md:471` tells a reader wanting bit-for-bit reproducibility to pin `uses: BellmeJoe/getadvantage-cli@v0.12.1` above a tag enumeration ending at `v0.12.1`. Live is **0.13.1**. Re-verified this cycle 2026-08-16T05:55:49Z: npm `version`/`dist-tags.latest` **0.13.1**, registry `gitHead` `e5b06f3`; tags `v0.12.2`→`1e451ac`, `v0.13.0`→`bdc8b04`, `v0.13.1`→`e5b06f3` all exist and are **all omitted**; the string `0.13` appears **zero** times in `README.md`; floating `v1`→`e5b06f3` so the primary copy-paste path is **correct**. **Harm, stated precisely:** only the reproducibility-pin example and the enumeration are stale — but a careful reader following them verbatim pins to `v0.12.1` and silently misses `0.13.1`, whose entire content is the repair of the paste-ready `secrets.ignore` JSON that shipped **invalid** in `0.13.0`. The stale pin routes a careful user onto a known-defective version. **The instruction that prevents recurrence:** this is the second stale-external-fact defect on this product in one day, so writing `v0.13.1` in place of `v0.12.1` reproduces the identical defect on the next release — **prefer a formulation that cannot go stale** (releases page / floating tag; drop the hand-maintained enumeration), and state which was chosen and why. **Hard scope:** `README.md` + this row **only**. **Forbidden:** any `*.mjs`, `action/`, `action.yml`, `package.json`, `ops/`, `tests/run.mjs`; version bump, tag, publish, Release, `SOCIAL-MEDIA-PACK.md`; new dependency. Never re-expose RLS; never claim Cursor; never claim adoption. **Gates (blocking):** product-surface diff empty; evidence **8/8**; pack **34 files** — **GATE CORRECTED MID-LANE by the dispatch routine:** it originally demanded a shasum byte-identical to `0685572c04b7769c4bed06232566bbb7d375ca7b` on the reasoning "docs are absent from the tarball". That reasoning belongs to the *previous* lane, whose file lived under `docs/`. **`README.md` is at repo root and is explicitly listed in `package.json` `files`** (`["*.mjs","action.yml","action/","tests/","README.md"]`; `npm pack --dry-run --json` → `entryCount: 34`, README packed `true`, `docs/` packed `false`), so for this lane the shasum **must** change and an unchanged shasum would mean the fix never reached users. Corrected gate: **entry count exactly 34, `README.md` the only member whose content moves**. The HEAVY candidates surfaced this discrepancy and reasoned about *why* the shasum moved instead of quietly passing the gate or tuning the work until it matched — it caught an error the dispatch routine introduced; full suite held at **257**; first screen **56 lines / verdict 53 / 5 `file:line` / 0 B stderr / exit 1**; every version string re-verified against the live registry and `git for-each-ref` in the same run with a UTC timestamp. Brief: `agent-ops/GROK-NEXT-TASK.md`. | **done** — closed 2026-08-16 12:45 at product fingerprint **`9223ce5`** (docs pin `750258d`) on independent audit **`REVIEW_GO (lane-scoped)`**, **0 P1 / 0 P2**, one pre-existing disclosed P3 not introduced by this candidate — `agent-ops/reviews/2026-08-16-claude-cli-audit-am.md`. The auditor independently re-read the diff, confirmed `grep -n "@v0\.[0-9]" README.md` → **no match** (only `@v1` ×2 survive), probed the releases URL the fix now points at (**HTTP 200**, `gh api` listing `v0.13.1, v0.13.0, v0.12.2, v0.12.1, v0.12.0` at the top), re-ran the **full suite to completion: 257/257, exit 0**, evidence **8/8 GREEN**, parsed the pack manifest with `JSON.parse` rather than grep (**34 files / 344,934 B / shasum `1df7434d…`**, `README.md` packed `true` at 36,587 B — and explicitly recorded that the shasum **move is the correct signal** the fix reached the tarball, independently confirming the mid-lane gate correction), re-read registry/tag truth live (`0.13.1` / `0.13.1` / `e5b06f3`; `v0.13.1` and `v1` both peel to `e5b06f3`), and re-ran the dogfood invariant (**56 lines / verdict header 53 / 5 `file:line` / exit 1 / 0 B stderr**, tree unmutated). Candidate stable across the audit (HEAD `750258d`, empty porcelain, `git diff HEAD | sha256sum` = the empty-tree hash at both acquisition and verdict). **Stop-loss closed at 0 of 3** — the lane never took a NO_GO. **Explicitly NOT release-eligible** — docs-only; the verdict authorizes closing this lane only. **Pushed to `origin/main` 2026-08-16 12:45** under the standing reviewed-commit push authority (`CANONICAL-FACTS.md`: a lane may push an already-reviewed commit provided the push adds **no version bump and no tag**) — no version bump, no tag, no publish, no Release. · Lane history retained below as evidence: **REVIEW_PENDING** at fingerprint **9223ce5** — Grok 4.5 HEAVY best-of-5 selected **candidate 5** (2026-08-16). **Formulation:** drop hand-maintained tag enumeration entirely; keep primary `@v1`; for bit-for-bit pin point at https://github.com/BellmeJoe/getadvantage-cli/releases with `uses: …@<tag>` placeholder — **cannot go stale** (writing a literal `v0.13.1` would reproduce the defect next release). **Why:** releases page is the single source of truth; README no longer lists tags. Verified 2026-08-16T05:59:53Z: npm `0.13.1` / dist-tags.latest `0.13.1` / gitHead `e5b06f3`; `v0.12.1`→`e2f8665`, `v0.12.2`→`1e451ac`, `v0.13.0`→`bdc8b04`, `v0.13.1`→`e5b06f3`, `v1`→`e5b06f3`. Scope: `README.md` + this row only. **Not done/LIVE; not release-eligible** — docs-only, rides next release train. Gates + dogfood recorded in candidate commit message / summary. Stop-loss **0 of 3**. · **CONVERGENCE, verified at the diff level by the dispatch routine rather than taken from the leader's narration: all five candidates independently removed the enumeration** and pointed at the releases page (`fa6a863`, `804061a`, `1f0e3ff`, `3bb8195`, `cf52732` — each 2 files, README +5/−4, lane row 2), each writing the "do not hand-list tags / they go stale" rationale into the README prose so a future editor cannot reintroduce the list without reading why it went. **Zero of five took the cheap route of substituting `v0.13.1` for `v0.12.1`.** Coordinator's stated basis for candidate 5: C1 and C3 fixed only the prose and left no concrete copy-paste pin form, while C2/C4/C5 supplied `uses: …@<tag>`; C5 kept `Prefer @v1` without over-restating the YAML block and ran the strongest cold-path dogfood. **The applied text contains no version number at all** — the defect class is "internally consistent, externally out of date", and this formulation has nothing left that can drift. · **ALL GATES INDEPENDENTLY RE-RUN BY THE DISPATCH ROUTINE, NOT ACCEPTED FROM THE LEADER:** product-surface diff `33f6d14..HEAD` over `*.mjs`/`action/`/`action.yml`/`package.json`/`tests/`/`ops/` **empty**; applied diff exactly 2 files (`README.md` 9 (+5/−4) + this row); `npm run evidence` **8/8 GREEN**; pack **34 files / 344,934 B / shasum `1df7434d6bc025a5409bbf2160a434cb3c37af52`** (changed, correctly — README packed `true`, `docs/` packed `false`); **full suite re-run to completion at `9223ce5`: 257/257 scenarios passed, `SUITE_EXIT:0`, 0 line-anchored FAIL** (`agent-ops/runs/suite-2026-08-16-0845-9223ce5.log`) — arithmetic identical to the `9efe0c3` base, the expected result for a documentation-only change and the evidence that it is one; first screen on a clean tree **56 lines / verdict header 53 / 5 `file:line` / 0 B product stderr / exit 1 / 1.14s**; `grep -n "@v0\.[0-9]" README.md` → **no match** (no stale exact-version Action pin survives); the releases URL the fix now points at returns **HTTP 200**. · **Dogfood triad, all read-only, run by the dispatch routine:** cold published `0.13.1` **exit 0 / 12.27s / 24 lines / 0 B product stderr / 0 RLS / 0 raw fatal**, verdict `GO — with 1 warning`, suggested next action usable (the 208 B on stderr is npm's own new-version notice, not product output); CLI repo as above, tree provably unchanged (HEAD + empty-porcelain hash identical before and after); site `C:\Users\ben\projects\getadvantage` **exit 1 / 82 lines / verdict 79 / 9.36s / 0 RLS / 0 raw fatal**, only the benign 147 B legacy `.ship-safe/` notice, **provably unmutated** (HEAD `c3040b06a415b3ad7a7d9fb4acfb764bbc2a55bb`, 13 entries, porcelain sha256 `70be9aeb75a8fc71` identical before and after). Every deterministic number matches its prior recorded value; **no new friction**. · Marketplace re-probed a **sixth** independent time 2026-08-16T06:52:51Z: **200/404/404**, unchanged; shelf test still evaluates **2026-09-14**. · **Residual finding, deliberately NOT fixed here:** `README.md:195` still reads "npm **0.12.x** does **not** check Supabase RLS" while live is **0.13.1** — not false (RLS is genuinely still unexposed in 0.13.x, confirmed by **0** RLS lines in all three dogfood legs) but scoped to a version nobody runs. Editing a tree at `REVIEW_PENDING` would invalidate the audit in flight, and it is outside this lane's declared scope. It is the **third instance of one defect class in two days**, so it is staged as a **class sweep** (`agent-ops/GROK-NEXT-TASK-STAGED.md`, Lane C), not a third one-off correction — fixing instances individually has now failed twice. · live unmoved **0.13.1** / `e5b06f3`; rollback **0.12.2** / `v0.12.2` / `1e451ac` · no push / tag / publish / Release / version bump / `SOCIAL-MEDIA-PACK` edit · never re-expose RLS · never claim Cursor · never claim adoption | 2026-08-16 |

| `0.13.x-version-claim-currency-sweep` | Grok Build | **Lane C — promoted 2026-08-16 12:45 on an empty board** after `0.13.x-readme-release-currency` closed `done`. **The class, not a fourth instance.** Four instances of one defect shape in two days: (1) `MARKETPLACE-LISTING.md` "Live evidence" table stale inside ~90 minutes (took a `REVIEW_NO_GO`); (2) `README.md:471` pinned `@v0.12.1` three releases behind live (fixed by Lane A at `9223ce5`); (3) `README.md:195` "npm **0.12.x** does **not** check Supabase RLS" while live is `0.13.1`; (4) **found live this cycle 2026-08-16T09:56Z, and this is the one that matters** — `docs/launch/SOCIAL-MEDIA-PACK.md` **L251** (`` `github-action` … `uses: …@v1` or `@v0.12.2` ``) and **L253** (`First-party Action … **live 0.12.2** · `@v1` / `@v0.12.2` `) both carry `@v0.12.2` pins **under a `# … getadvantage 0.13.1 LIVE` header on line 1**. That file is the *sole canonical CLI advertising truth* per `CANONICAL-FACTS.md`, so a stale pin there is an owner-truth defect on the one document every other lane quotes, not a cosmetic docs nit. Measured surface: **31** version-string hits in the pack, **10** in `README.md`. **Fixing instances one at a time has now failed three times** — Lane A proved the right move is to delete the staleable construct rather than update it, and this lane applies that move to the whole class at once. **Hard scope:** `README.md` + `docs/launch/SOCIAL-MEDIA-PACK.md` + this row **only**. **Forbidden:** any `*.mjs`, `action/`, `action.yml`, `package.json`, `ops/`, `tests/run.mjs`; version bump, tag, publish, Release; new dependency. **Never weaken a safety disclaimer while de-versioning it** — the RLS sentence must still say plainly that RLS is not checked and not advertised; only its version scoping may change. Never re-expose RLS; never claim Cursor; never claim adoption. **Deliverable is a disposition table of every instance found** — (a) drop the version scope where the claim is version-independent, (b) point at a self-updating live surface (releases page / `dist-tags`), or (c) keep a literal version **only** where it is genuinely historical and mark it as such — an instance found and left must be listed with its reason. **Boundary rule:** if de-versioning would make a claim inaccurate, keep the version, mark it historical, and report it; do not widen scope. **Gates (blocking):** product-surface diff empty; evidence **8/8**; pack **34 files** and — stated correctly this time — the shasum **must** change **iff `README.md` moves** and **must not** change if only the pack moves (`docs/` is absent from the tarball; `README.md` is in `package.json` `files`); full `npm test` held at **257**, arithmetic shown; first screen **56 lines / verdict 53 / 5 `file:line` / 0 B stderr / exit 1**; every surviving version string re-verified against the live registry and `git for-each-ref` in the same run with a UTC timestamp. Brief: `agent-ops/GROK-NEXT-TASK.md`. | **done** — closed 2026-08-16 16:45 at product fingerprint **`397b8cd`** on independent audit **`REVIEW_GO (lane-scoped)`**, **0 P1 / 0 P2 / 0 P3 introduced** — `agent-ops/reviews/2026-08-16-claude-cli-audit-midday.md`. The auditor independently read the full diff, confirmed `grep -n "@v0\.[0-9]"` → **no match in either owned file** (README version-string count **10 → 0**), verified the product-surface diff empty from scratch, re-ran the **full suite to completion: 257/257, exit 0, 0 FAIL**, evidence **8/8 GREEN**, parsed the pack manifest with `JSON.parse` (**34 files / shasum `fc68acbe2eaf82edd0134f76d3109bfa27b344a4`**, README packed `true` at 36,641 B, `docs/` packed `false` — exact match to the candidate's recorded numbers), re-read registry/tag truth live (`0.13.1` / `0.13.1` / `e5b06f3`), re-ran the dogfood invariant (**56 / 53 / 5 / 0 B / exit 1**, tree unmutated), and independently confirmed the BOM-only amend by tree hash (`6fcbd9e^{tree}` == `397b8cd^{tree}` == `9553260…`, `git diff` empty). It specifically graded the safety constraint: the RLS disclaimer **gained** three clauses and lost only its dead version scope — **not weakened**. Candidate stable at both ends of the audit. **Stop-loss closed at 0 of 3** — the lane never took a NO_GO. **Explicitly NOT release-eligible** — docs-only; the verdict authorizes closing this lane only. **Pushed to `origin/main` 2026-08-16 16:45** (`56c2bbf..397b8cd`) under the standing reviewed-commit push authority (`CANONICAL-FACTS.md`: a lane may push an already-reviewed commit provided the push adds **no version bump and no tag**); pre-push guard `git diff --name-only origin/main..HEAD -- package.json` → **empty**, no tag pushed. · Lane history retained below as evidence: **REVIEW_PENDING** — docs-only class sweep applied (candidate 4 of 5). **Header rule:** one declared live-version block in `SOCIAL-MEDIA-PACK.md` (title + Live callout + Field table), kept current by the release contract; **zero** copy-pasteable exact-version Action/npm pins outside that block; floating `@v1` + [releases page](https://github.com/BellmeJoe/getadvantage-cli/releases) for pins. **Critical fixes:** pack L251/L253 dropped `@v0.12.2` → `@v1` / releases; README L195 dropped `npm **0.12.x**` → version-free "Shipped npm does **not** check Supabase RLS" (safety not weakened). **Disposition:** (a) drop version scope on live feature markers / RLS; (b) point Action pins at `@v1` + releases; (c) keep live-block declared version + historical "Still live from … (historical intro)" + rollback + README `pre-0.9.0` / `### 0.6.0` changelog. **Shasum branch:** README.md moved → shasum **must** change (`docs/` not packed). Suite arithmetic: base **257** at `9223ce5` + 0 new scenarios = **257**. Verified 2026-08-16T10:38:44Z: npm `0.13.1` / dist-tags.latest `0.13.1` / gitHead `e5b06f3`; tags `v0.12.2`→`1e451ac`, `v0.13.0`→`bdc8b04`, `v0.13.1`→`e5b06f3`, `v1`→`e5b06f3`. Best-of-5 isolated HEAVY selected **candidate 4** (strongest class sweep: historical markers on Still-live intros + rollback; full RLS safety retained; zero `@v0.x.y` copy-paste pins outside live block). Stop-loss **0 of 3**. Stopped at **REVIEW_PENDING**; not release-eligible; rides next release train. Live **0.13.1** / `e5b06f3`; rollback **0.12.2** / `v0.12.2` / `1e451ac`. No tag / publish / Release / version bump. · **Measured effect at close:** copy-pasteable `@v0.x` pins **2 → 0** in the pack, **0 → 0** in the README; version-string hits **31 → 22** (pack) and **10 → 7** (README) per the dispatch routine, with the auditor independently reading README `0\.1[0-9]\.[0-9]` at **0** and the pack's **24** survivors all inside the one declared live-version block or explicitly labelled historical (the two counts use different regexes and are not in conflict). | 2026-08-16 |

| `0.13.x-evaluator-feedback-loop` | Grok Build | **Lane B — bet 14, promoted 2026-08-16 16:45 on an empty board** after `0.13.x-version-claim-currency-sweep` closed `done`. **Why this and not something higher-scoring:** bet 14 scores **1.08** and is the highest-ranked *promotable, non-send* item. The two above it are both blocked on things no agent may do — **16a** (3.55) is a live Marketplace listing whose only remaining step is a **founder click** (Developer Agreement + publish checkbox), and **16b** (2.85) needs a **PR to a third party's repository**, which is send-class and sits inside the send gate, shut 22 days and reaffirmed by the founder 2026-08-14. Both recorded rather than silently dropped. **The argument:** the Marketplace listing is live (200, probed seven times) and the 30-day shelf test evaluates **2026-09-14**. If someone arrives from it, runs the gate, and hits something wrong, **there is no route by which we would ever learn** — the repository has **zero issues in its lifetime**. This builds the inbound route before the shelf window closes. **Its honest weakness, kept on the board so nobody has to rediscover it:** it serves a population of **zero today**, and a feedback link nobody clicks is worth nothing; it is justified as instrumentation for a live experiment, never as a growth mechanism. **What it does:** `getadvantage feedback` prints a copy-pasteable GitHub issue URL pre-filled with redacted environment and result metadata (CLI version, Node version, OS platform — **not** hostname, **not** username, detected stack, per-check verdict counts, check ids). **It prints the link. It never opens a browser, never posts anything, and makes zero network requests** — a hard property, not a preference, and what keeps this lane out of the send gate. **Hard scope:** `feedback.mjs` (new, pure function, no I/O, no network) + `index.mjs` (one command wire, nothing else) + `tests/run.mjs` (`TEST_FILTER=feedback`) + `README.md` (one short section, **written unversioned** — Lane C's rule now binds new text too) + this row. **Forbidden:** `checks.mjs`, `checks-runner.mjs`, `policy.mjs`, `report.mjs`, `sarif.mjs`, `action/`, `action.yml`, `package.json`, `ops/`; version bump, tag, publish, Release, `SOCIAL-MEDIA-PACK.md`; new dependency; telemetry; any network call from product code; any change to a verdict, exit code, `--json` field shape, or SARIF. **Boundary rule:** if producing a correct link requires touching a check module, adding a dependency, or making any network request — **stop and report**. **Ship blockers (mutation-proven — neuter the redaction or the cap in a scratch copy and the corresponding scenario must fail):** (1) secret value never travels — absent from stdout, stderr, raw URL **and** the URL after `decodeURIComponent`, exact substring; (2) username / absolute path never travels; (3) overlong payload truncates under a named exported `MAX_URL_CHARS` with truncation visibly disclosed, never an invalid URL; (4) **zero network proven** by injected `fetch`/`https.request` counter reading exactly **0** at child exit (reuse the `--report-dry-run` proof shape); (5) no remote / non-git / detached HEAD degrade cleanly — exit **0**, zero raw git noise, no `fatal:` reaches the user; (6) **never fabricates a target** — host and repo path are constants in `feedback.mjs`, never derived from the user's `origin` (a derived target would send a stranger's bug report to a stranger's repo); (7) private-repo hygiene; (8) regression pins — `check` first screen and byte-identical `--json`/SARIF against the pre-lane baseline. **Gates (blocking):** full `npm test` green with **arithmetic shown against base 257**; evidence **8/8**; pack stays exactly **34 files** with `feedback.mjs` shipping and tests/docs not — README moves so the shasum **will** change, which is the correct signal, not a red flag; first screen unchanged at **56 lines / verdict 53 / 5 `file:line` / 0 B stderr / exit 1**; dogfood triad re-run read-only with no new friction. **Cold-path criteria:** `feedback` exit **0** on a clean repo *and* after a NO-GO run (it is not a gate); **< 10s** cold / **< 3s** warm with an isolated npm cache placed **outside** the test repo (a cache inside the repo registers as untracked files and an allowlisted hit on npm's own cached fixtures — a measurement artifact, not product behaviour, observed and corrected 2026-08-16 12:45); first screen **≤ 12 lines**; link parses with `new URL()` with origin+path exactly `https://github.com/BellmeJoe/getadvantage-cli/issues/new` and all payload in query params. Brief: `agent-ops/GROK-NEXT-TASK.md`. | **done** — closed 2026-08-18 08:45 at product commit **`8eb494b`** on independent audit **`REVIEW_GO (lane-scoped)`**, **0 P1 / 0 P2 / 0 P3** — `agent-ops/reviews/2026-08-17-claude-cli-audit-evening.md`. Verdict bound to reviewed fingerprint `e68cef1` + diff sha256 `84c5e2f5499d4883ac424c80de2c58f8bbb369036ff681d82a24337e18c9637e` (blobs: `feedback.mjs` `0cb6a06d`, `index.mjs` `dd8e0a88`, `tests/run.mjs` `2972fe16`, `README.md` `506fd065`); product commit preserves those reviewed bytes. **Explicitly NOT release-eligible** — a lane-scoped GO authorizes closing this lane only; **rides the next release train** and did **not** trigger one. No version bump / tag / publish / Release / `SOCIAL-MEDIA-PACK`. Live unmoved **0.13.1** / `e5b06f3`; rollback **0.12.2** / `v0.12.2` / `1e451ac`. · Lane history retained: **REVIEW_PENDING** — best-of-5 selected **candidate 1** (coordinator applied 2026-08-17; base 4a5b564). TOC mid-file insert dropped so secret startLines stay pin-stable after commit. Suite 257+10=267 green; evidence 8/8; pack 35 (feedback.mjs ships). Post-lane clean overlay: first screen 56/53/5/0B/exit1, SARIF 3aab5a242c7a5986, JSON excl generatedAt cb9fec3fe5aa5cb0. Product: new `feedback.mjs` (pure builder; constants `FEEDBACK_*` + `MAX_URL_CHARS=7000`; `buildFeedbackUrl` / redact / truncate) + `index.mjs` wire (`feedback` before hard `repoRoot()` fail; silent nested `runChecks` for metadata; always exit 0; ≤12-line screen; 0 B stderr) + `tests/run.mjs` **10** hostile scenarios under `TEST_FILTER=feedback` (secret never travels + mutation-neuter; path/username; cap + mutation; zero network mock server; non-git/no-remote/detached; constant target; private-repo hygiene; first-screen density; regression pins via clean detached worktree of HEAD — 56/53/5/0B/exit1, SARIF prefix `3aab5a242c7a5986`, `--json` excl. generatedAt prefix `cb9fec3fe5aa5cb0`) + README unversioned section + this row. **Gates:** `TEST_FILTER=feedback` **10/10**; full `npm test` **267/267** (= base **257** + **10** feedback); `npm run evidence` **8/8 GREEN**; pack **entryCount 35** (pre-lane 34 + root `feedback.mjs`; README shasum moves — correct). Never re-expose RLS; never claim Cursor; never claim adoption. | 2026-08-18 |

| `0.13.x-retained-team-detector-incomplete-results` | Grok Build | **P3-1 — implemented 2026-08-18; terminal state REVIEW_PENDING.** Staged brief: `agent-ops/GROK-NEXT-TASK-STAGED.md`. **Measurement repair, not a product feature.** Closes the carried P3-1 on `ops/retained-team-detector.mjs`: the detector emits a confident `0` / exit `0` when it cannot distinguish "nobody uses this" from "search is not answering". **Implemented (req 0–6):** production `incomplete_results: true` → `UNKNOWN` + exit 1, reason contains `code search returned incomplete results`, never a numeric count (mutation-proven). Control-first world-fact query `filename:package.json left-pad` (exported `CONTROL_QUERY`; **not** repo-scoped — live measurement showed `repo:BellmeJoe/…` returns `incomplete_results: true` even when healthy). Control judged by count only; failing control not rescued by production hits; control-pass + production `{total_count:0, incomplete_results:false}` → plain `0` / exit 0. Output text distinguishes observed absence / instrument failure / missing token. **What is NOT broken — do not fix it.** The HTTP error path already works (live 503 → `UNKNOWN`, exit 1, `reason: code search HTTP 503`); missing token → `UNKNOWN`, exit 1, `reason: missing GITHUB_TOKEN`, including *"This is not a retained-team count of 0."* Two of three failure paths are already correct; preserve them. The false-zero was observed on the `incomplete_results` path specifically. **Hard scope:** `ops/retained-team-detector.mjs` + `tests/run.mjs` (`TEST_FILTER=retained-team`) + this row. **Forbidden:** any product `*.mjs` outside `ops/`, `action/`, `action.yml`, `package.json`, `README.md`, `SOCIAL-MEDIA-PACK.md`; version bump, tag, publish, Release; new dependency; telemetry; any user-facing CLI. `ops/` must stay absent from `package.json` `files`. **Terminal state when worked:** `REVIEW_PENDING`. Not release-eligible. **Shipped in-lane:** production `incomplete_results: true` → `UNKNOWN` + exit 1 (`code search returned incomplete results`); control-first world-fact query (not repo-scoped); control count-only; real zero still plain `0` when control passes; three outcome texts distinguished. Never re-expose RLS; never claim Cursor; never claim adoption (a "validated zero" requires a passing positive control). | **REVIEW_PENDING** — implementation complete in this worktree (control-first world-fact `filename:package.json left-pad` + production `incomplete_results → UNKNOWN`). Hard scope only: `ops/retained-team-detector.mjs`, `tests/run.mjs` (`TEST_FILTER=retained-team`, 20/20), this row. Not push/publish/release-eligible. Live **0.13.1** / `e5b06f3`; rollback **0.12.2** / `v0.12.2` / `1e451ac`. | 2026-08-18 |

> **RLS lane parked (2026-07-29):** eight consecutive independent `REVIEW_NO_GO`
> verdicts (`fdb81a4` … `d246171`); unbounded adversarial Postgres DDL with no
> convergence after six instance + one class-level repair; zero real-repo RLS
> evidence after fifteen cycles (synthetic fixtures only). NORTHSTAR:125 kill
> criteria met. Code left in place and **unexposed** from `runChecks`/`gateTree`
> at product fingerprint **`cef3491`** (lane `0.11.x-unexpose-parked-rls`,
> **released unexposed** with **0.11.0** — unexposing is not repairing; open
> P1/P2 remain). Live is **0.12.2** (product fingerprint `653a693`; prior live
> **0.12.1** / `e2f8665` is the rollback target). **Never advertise RLS as live.**

### Rules
1. Before starting: add a row.  
2. Same path already open? **Don’t start a parallel suite** — join or wait.  
3. When done: set Status `done` or delete the row + write a `docs/sessions/…-SESSION.md` (kept local — gitignored, not published).  
4. Canonical tools: evidence = `npm run evidence` · launch HTML = `docs/launch/` only.

### Example rows

| Lane | Owner (tool/person) | Path / concern | Status | Updated |
|------|---------------------|----------------|--------|---------|
| launch-visual | Grok | docs/launch/ | done | 2026-07-19 |
| evidence-gate | Claude | ops/evidence-suite.mjs | done | 2026-07-19 |
